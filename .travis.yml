sudo: required
dist: xenial
language: python
python: 3.7

install:
  - sudo apt update
  - sudo apt install docker.io -y
  - sudo docker build --rm -f "Dockerfile" -t driplet:latest .
  - sudo pip install requests

before_script:
  - sudo docker run -d -p 3141:3141 driplet

script:
  - sudo python backend/tests/acc_create.py